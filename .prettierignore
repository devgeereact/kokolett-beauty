# Build output and dependencies
dist/
dev-dist/
node_modules/
coming-soon/

# Agent tooling scratch
.claude/
.claude-flow/
.swarm/
.agents/

# Generated by `supabase gen types typescript`. Prettier reformats it into a
# shape the generator does not produce, so every regeneration reintroduced a
# 2,400-line diff and turned the CI `format:check` step red. The file is machine
# output; it is not ours to style.
src/types/database.types.ts
